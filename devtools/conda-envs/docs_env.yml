name: docs
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

  # Imports
  - gitpython
  - toml
  - typer

  # Docs depends
  - sphinx >=4.5,<5.0
  - sphinx_rtd_theme
  - myst-parser>=0.13.6
  - sphinx-notfound-page
  - sphinx-click
