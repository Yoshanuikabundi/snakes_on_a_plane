name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # Imports
  - gitpython
  - toml
  - typer[all]>=0.6
  - pydantic~=2.0.0
  - pyyaml

  # Testing
  - pytest
  - pytest-cov
  - codecov
