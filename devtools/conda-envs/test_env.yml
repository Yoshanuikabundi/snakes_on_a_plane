name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # Imports
  - gitpython
  - toml
  - typer
  - schema
  - pyyaml

  # Testing
  - pytest
  - pytest-cov
  - codecov
